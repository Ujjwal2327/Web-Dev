export const baseUrl = "https://codehelp-apis.vercel.app/api";

/*
    next and previous
        ${baseUrl}/get-blogs?page=${page}
    
    blog heading
        ${baseUrl}/get-blog?blogId=${BLOG1100}
        
    tag
        ${baseUrl}/get-blogs?page={page}&tag=${Problem-Solving}

    category
        ${baseUrl}/get-blogs?page=${page}&category=${category}

    %20
        space

*/